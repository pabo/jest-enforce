{
  "name": "jest-enforce",
  "version": "1.0.0",
  "description": "check whether you're mocking what you should",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "find-in-files": "~0.4.0",
    "recursive-readdir": "~2.2.1"
  },
  "bin": {
    "jest-enforce": "bin/jest-enforce.js"
  }
}
